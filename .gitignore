# Local high-res source artwork — originals kept on disk, never deployed.
# (Member portraits used by the site live in /images/ as optimized .jpg files.)
/*.png
/*.PNG
!/chapters-map.png

# One-time migration tool and editor/agent state — not part of the deployed site.
build-migrate.js
.claude/
site/

# Wrangler local dev state (created by `npx wrangler dev`).
.wrangler/

# Local preview helper — never deployed.
.preview-server.js

# Local-only dev secrets. Never deployed, never committed.
.dev.vars

# Node dependencies for the Worker bundle (wrangler bundles at deploy time).
node_modules/
